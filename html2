<!doctype html>
<html lang="en">
<head>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <title>Adobe Analytics Data Extractor</title>
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.2.2/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-Zenh87qX5JnK2Jl0vWa8Ck2rdkQ2Bzep5IDxbcnCeuOxjzrPF/et3URy9Bv1WTRi" crossorigin="anonymous">
    <style>
        body {
            padding: 20px;
        }
        
        h1 {
            font-size: 24px;
            font-weight: bold;
            text-align: center;
            margin-bottom: 30px;
        }
        
        label {
            font-weight: bold;
        }
        
        select.form-control,
        input[type="checkbox"] {
            margin-bottom: 10px;
        }
        
        .checkbox label {
            font-weight: normal;
        }
    </style>
</head>
<body>
    <h1>Adobe Analytics Data Extraction API</h1>
    <div class="row">
        <div class="col-md-4"></div>
        <div class="col-md-4">
            <form action="{{ url_for('read_form')}}" method="post">
                <label for="dimension">Dimension</label>
                <select name="inputDimension" class="form-control" id="dim">
                    <option></option>
                    <!-- Options omitted for brevity -->
                </select>
                <br>
                <label for="metrics" class="form-label">Metrics</label>
                <div class="checkbox">
                    <label><input type="checkbox" name="inputMetrics" value="Exits">Exits</label>
                </div>
                <div class="checkbox">
                    <label><input type="checkbox" name="inputMetrics" value="Average Time Spent on Page (seconds)">Average Time Spent on Page (seconds)</label>
                </div>
                <!-- Additional checkboxes omitted for brevity -->
                <br>
                <button type="submit" class="btn btn-primary">Submit</button>
            </form>
        </div>
        <div class="col-md-4"></div>
    </div>
</body>
</html>
